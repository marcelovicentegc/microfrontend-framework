import { AppConfig } from "@mf-framework/config";

export default {
  basePath: "/nextjs-template-app",
  items: [
    {
      route: "/",
      title: "NextJS Template App",
      pageName: "index.tsx",
    },
  ],
} as AppConfig;
